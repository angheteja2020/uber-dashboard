// Estimated operational cost per mile (combines fuel, maintenance, etc.).
export const costPerMile = 0.17;

// Minimum acceptable net profit (USD) for an order to be considered viable.
export const minNet = 7.5;

// Minimum hourly rate (USD) that qualifies an order as profitable.
export const minRate = 23.0;
